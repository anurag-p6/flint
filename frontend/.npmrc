# Privy's optional smart-account deps (permissionless → ox) declare an ox range
# disjoint from viem's. Nothing in our flows touches that path, so peer
# resolution is relaxed. This also keeps `npm ci` green on deploy builders.
legacy-peer-deps=true
